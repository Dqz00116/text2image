---
id: FEAT-499776f9
req: REQ-499776f9
status: draft
---

# Multi-Image Support — Technical Implementation

## Changes

### client.py
- `_build_messages(prompt, image_paths: list[str] | None)` — iterate paths, build N `image_url` parts
- `generate(image_paths: list[str] | None = None)` — rename param from `image_path`

### cli.py
- `--image` / `-i` → `multiple=True`, type=list
- Pass `image` list to `client.generate(image_paths=image)`

### Approach
Single approach (no alternative needed): extend existing one-image path to loop over list.

## Test updates
- Test multi-image message structure
- Test single-image backward compat (existing tests cover this)
